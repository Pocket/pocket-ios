query unleashAssignments($context: UnleashContext!) {
    assignments: unleashAssignments(context: $context) {
        assignments {
            name
            assigned
            variant
            payload
        }
    }
}
